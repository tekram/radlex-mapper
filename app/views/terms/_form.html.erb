<%= form_for @term, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :rid, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :rid, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :rid_parent, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :rid_parent, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                terms_path, :class => 'btn' %>
  </div>
<% end %>
