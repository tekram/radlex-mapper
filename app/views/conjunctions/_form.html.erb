<%= form_for(@conjunction) do |f| %>
  <% if @conjunction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conjunction.errors.count, "error") %> prohibited this conjunction from being saved:</h2>

      <ul>
      <% @conjunction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
